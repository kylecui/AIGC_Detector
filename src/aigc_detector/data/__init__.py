"""Data pipeline modules for AIGC Detector.

Submodules:
    crawler   — Human text collection (Wikipedia API + HC3 dataset)
    processor — Text cleaning, filtering, deduplication
    generator — Multi-model AI text generation
    mixer     — Mixed text construction (AI completion + sentence insertion)
    splitter  — Train/val/test dataset splitting
"""
